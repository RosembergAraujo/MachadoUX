<!doctype html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@100;400;500;700&family=Open+Sans&display=swap"
      rel="stylesheet"
    />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <title>Machado UX</title>
    <meta name="description" content="Machado UX by Maria Clara Machado" />

    <meta property="og:title" content="Machado UX" />
    <meta property="og:url" content={`https://${import.meta.env.VERCEL_URL}`} />
    <meta
      property="og:image"
      content={`https://${import.meta.env.VERCEL_URL}/og-image.png`}
    />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="pt_BR" />
    <meta
      property="og:description"
      content="Machado UX by Maria Clara Machado"
    />
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global>
  :root {
    --background: #f5f5f5;
    --white: #ffffff;

    --text-darker: #1d1d1e;
    --text: #6a6a6a;

    --wrapper-padding-inline: 1rem;
    --wrapper-max-width: 100rem;

    --section-padding-block: 4rem 5rem;
  }

  * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
  }

  html {
    font-family: Inter, sans-serif;
    color: var(--text);
  }

  body {
    scroll-behavior: smooth;
  }

  a {
    text-decoration: none;
    color: inherit;
  }

  li {
    list-style: none;
  }

  .wrapper {
    max-width: var(--wrapper-max-width);
    margin-inline: auto;
    padding-inline: var(--wrapper-padding-inline);
  }

  .full-bleed {
    padding-block: 0;
  }

  .full-bleed > img {
    margin-top: -8rem;
    width: 100%;
    max-height: 400px;
    object-fit: contain;
    object-position: right;
  }

  .full-bleed > :not(img) {
    padding-block: var(--section-padding-block);
    padding-inline: var(--wrapper-padding-inline);
  }

  @media screen and (min-width: 1220px) {
    .full-bleed {
      display: grid;
      grid-template-columns:
        minmax(var(--wrapper-padding-inline), 1fr)
        minmax(0, calc(var(--wrapper-max-width) / 2))
        minmax(0, calc(var(--wrapper-max-width) / 2))
        minmax(var(--wrapper-padding-inline), 1fr);

      position: relative;
    }

    .full-bleed > img {
      margin-top: 0;
      width: 100%;
      max-height: 900px;
      object-fit: contain;
      object-position: right;
    }

    .full-bleed > img:first-child {
      grid-column: 1 / 3;
    }

    .full-bleed > img:last-child {
      grid-column: 3 / -1;
    }

    .full-bleed > :not(img) {
      display: grid;
      align-content: center;
      justify-items: start;
    }

    .full-bleed > :not(img):first-child {
      padding-left: 0;
      grid-column: 2 / 3;
    }

    .full-bleed > :not(img):last-child {
      padding-right: 0;
      grid-column: 3 / 4;
    }

    .full-bleed > :not(img)::before {
      content: "";
      position: absolute;
      inset: 0;
      background: inherit;
    }

    .full-bleed > :not(img):first-child::before {
      grid-column: 1 / 2;
    }

    .full-bleed > :not(img):last-child::before {
      grid-column: 4 / 5;
    }
  }
</style>
